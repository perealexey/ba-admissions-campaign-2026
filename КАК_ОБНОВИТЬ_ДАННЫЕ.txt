ИНСТРУКЦИЯ: КАК ОБНОВИТЬ ДАННЫЕ ПРИЁМНОЙ КАМПАНИИ БАКАЛАВРИАТА
(на случай если забыл(а) или запутался(ась))

Всё выполняется из терминала, из папки проекта:
cd "/Users/aleksejpereaslov/Documents/Work/institute_media/ba_summer_campaign_2026"


------------------------------------------------------------
ГЛАВНОЕ ОТЛИЧИЕ ОТ МАГИСТРАТУРЫ
------------------------------------------------------------
У pk.hse.ru (бакалавриат) НЕТ установленного ограничения "не чаще раза в
сутки" — это другой сайт с другими договорённостями, не как priem47.hse.ru.
Единственная соблюдаемая мера — задержка 2 сек между запросами (уже в
collector_bak.py). Но полный сбор всё равно долгий (491 обычная группа +
32 пула целевой квоты, десятки минут) — сборщик резюмируемый, можно смело
прерывать (Ctrl+C) и запускать заново, уже собранное не перескачается.


------------------------------------------------------------
ШАГ 1. Собрать свежие данные
------------------------------------------------------------
python3 scripts/collector_bak.py

Идёт долго. Прогресс — в bak_data/progress.json, лог — bak_data/collector.log.
Если прервалось на середине — просто запустите ту же команду ещё раз.


------------------------------------------------------------
ШАГ 2. Пересчитать метрики
------------------------------------------------------------
python3 scripts/campaign_metrics_bak.py bak_data/ data/supply_bak_raw.csv \
    --snapshot-date ГГГГ-ММ-ДД -o data/

(ГГГГ-ММ-ДД — сегодняшняя дата, например 2026-07-15)

Если раньше уже считали метрики и просто хотите пересчитать на новых данных
из ШАГА 1 — эта же команда, файл data/supply_bak_raw.csv менять не нужно
(см. ШАГ 2а ниже, когда он нужен).


------------------------------------------------------------
ШАГ 2а. ЕСЛИ ПОМЕНЯЛИСЬ МЕСТА ПРИЁМА (КЦП) — РЕДКО, НЕ КАЖДЫЙ РАЗ
------------------------------------------------------------
Если ВШЭ обновила RTF-таблицы мест приёма — сохраните новые файлы в docs/
(moscow_supply.rtf, nnov_supply.rtf, perm_supply.rtf, spb_supply.rtf,
поверх старых) и пересчитайте:

python3 scripts/parse_supply_bak.py docs/ -o data/

Скрипт сам сверяет сумму по строкам со строкой "Итого"/"Всего" в каждом
файле — если увидите предупреждение о расхождении, не игнорируйте, парсинг
мог съехать по колонкам.

Только потом запускайте ШАГ 2 (метрики).


------------------------------------------------------------
ШАГ 3. Залить на Google Drive
------------------------------------------------------------
python3 scripts/sync_to_drive_bak.py

Заливает содержимое data/ в приватную папку на Drive, откуда дашборд её
читает. Требует .streamlit/secrets.toml локально (см. ниже, где лежит).


------------------------------------------------------------
ШАГ 4. Проверить, что дашборд обновился
------------------------------------------------------------
Откройте ссылку на задеплоенный дашборд, введите пароль (см.
.streamlit/secrets.toml, app_password), посмотрите на строку под заголовком
"Данные актуальны на момент ДАТА" — там должна быть сегодняшняя дата.

Если дата старая — подождите 10 минут (у дашборда есть кэш на 10 минут) и
обновите страницу.


------------------------------------------------------------
ЕСЛИ НУЖНО ЗАПУСТИТЬ ТОЛЬКО МЕСТНО, БЕЗ ЗАЛИВКИ НА DRIVE
------------------------------------------------------------
Локальный запуск дашборда: streamlit run app.py
(нужен тот же .streamlit/secrets.toml — дашборд всегда читает данные из
Drive, даже при локальном запуске, отдельного "локального режима" нет)


------------------------------------------------------------
ГДЕ ЧТО ЛЕЖИТ / ЗА ЧТО ОТВЕЧАЕТ (если нужно копнуть глубже)
------------------------------------------------------------
docs/PIPELINE.md    — как устроен весь процесс, шаг за шагом, подробно
docs/METRICS.md     — что означают все метрики и формула объединённой желанности
docs/FINDINGS.md    — разбор находок по структуре данных (близнецы, целевая
                       квота, независимая нумерация приоритетов)
.streamlit/secrets.toml — пароль дашборда + ключ доступа к Google Drive
                          (этот файл НЕ в git, если потеряется — придётся
                          пересоздавать ключ в Google Cloud Console или брать
                          из summer_campaign_26/.streamlit/secrets.toml —
                          тот же сервисный аккаунт, другая папка на Drive)


------------------------------------------------------------
ЕСЛИ СОВСЕМ ЗАПУТАЛСЯ
------------------------------------------------------------
Ничего не сломается необратимо: локальные файлы (bak_data/, data/) никуда не
удаляются автоматически. В крайнем случае можно открыть новый чат с Claude
Code в папке проекта и попросить разобраться — docs/PIPELINE.md,
docs/METRICS.md и docs/FINDINGS.md дают всему контекст с нуля, а
docs/CONTEXT_FOR_FUTURE.md — рабочую память о принятых решениях и граблях
(этот файл не в git, только локально).
